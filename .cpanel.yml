---
deployment:
  tasks:
    - /usr/bin/rsync -av --exclude='.git' --exclude='.cpanel.yml' --exclude='.github' --exclude='.claude' --exclude='preview.html' --exclude='server.py' $DIRECTORY/ /home/lsjcloab/public_html/
