# Logs & user data
data/messages.log

# Secrets – never commit
private/secrets.php
data/settings.json
.sync-token

# Financial data & uploads
admin/statistici/data/pnl.sqlite
admin/statistici/data/clp.sqlite
admin/statistici/uploads/

# Preview & dev files
preview.html
server.py

# OS
.DS_Store

# Editor
.vscode/
.claude/
*.swp
*.zip

# Dependencies
node_modules/

# Andy course-task cron state (server-side)
data/andy_course_tasks_done.json


# Recurring tasks (server-side data)
data/recurring_tasks.json
data/recurring_done.json

# Instagram calendar posts (server-side data)
data/instagram_posts.json

# Course publish-task state (server-side)
data/course_publish_done.json

# Course ideas — cursuri posibile (server-side data)
data/course_ideas.json

# web/ (Next.js) — build output, env & local tooling (node_modules deja ignorat mai sus)
web/.next/
web/.vercel/
web/.env*.local
web/next-env.d.ts
web/tsconfig.tsbuildinfo

# migration/ — secrete
migration/.env
