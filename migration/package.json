{
  "name": "clp-migration",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Migrare date live Curs la Pahar (SQLite + JSON) -> Neon Postgres",
  "scripts": {
    "schema": "tsx src/schema.ts",
    "migrate": "tsx src/migrate.ts",
    "verify": "tsx src/verify.ts"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
